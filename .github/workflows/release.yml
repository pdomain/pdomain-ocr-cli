name: release

# Dispatch-only release. scripts/do-release.sh pushes the exact tag after
# local ci-slow passes, then triggers this workflow with the tag input below.
# Chore: review pinned action SHAs weekly. Update version comments and SHAs together.

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release, for example v1.2.3"
        required: true

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  UV_PYTHON: "3.12"
  CUDA_VISIBLE_DEVICES: ""

jobs:
  publish:
    name: publish GitHub Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v6.0.2
        with:
          fetch-depth: 0
          ref: ${{ github.event.inputs.tag }}
          persist-credentials: false
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v6.0.2
        with:
          repository: pdomain/pdomain-ops
          ref: 2d01f1d2fee7fbdb2bd5a19e28182e949330cfd3
          path: pdomain-ops
          persist-credentials: false
      - name: Link pdomain-ops path dependency
        run: ln -sfn "$PWD/pdomain-ops" ../pdomain-ops
      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9  # v8.1.0
        with:
          version: "0.11.32"
      - name: Set up Python
        run: uv python install 3.12
      - name: Check release dependency sources
        run: make check-release-deps
      - name: Build release artifacts
        run: uv build

      - name: List built artifacts
        run: ls -la dist/

      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ github.event.inputs.tag }}
        run: |
          set -euo pipefail
          shopt -s nullglob
          files=(dist/*.whl dist/*.tar.gz)
          if [ ${#files[@]} -eq 0 ]; then
            echo "::error::no release artifacts found under dist/"
            exit 1
          fi
          gh release create "$RELEASE_TAG" "${files[@]}" \
            --generate-notes \
            --verify-tag

      - name: Notify pd-index of new release
        env:
          GH_TOKEN: ${{ secrets.PDOMAIN_INDEX_DISPATCH }}
          RELEASE_TAG: ${{ github.event.inputs.tag }}
        run: |
          if [ -z "$GH_TOKEN" ]; then
            echo "::warning::PDOMAIN_INDEX_DISPATCH is not set; index will catch up via daily cron"
            exit 0
          fi
          gh api -X POST /repos/pdomain/pdomain-index-pip/dispatches \
            -f event_type=pdomain-release-published \
            -F 'client_payload[repo]=pdomain-ocr-cli' \
            -F "client_payload[tag]=$RELEASE_TAG" || \
            echo "::warning::pdomain-index-pip dispatch failed; index will catch up via daily cron"
